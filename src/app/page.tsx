import HomeScreen from "@/components/pages/home-screen/homescreen";
export default function Home() {
  // return <HomeScreen/>
  return <div>Just wait for a while we are pushing something new </div>
}
